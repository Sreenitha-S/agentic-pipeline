# Deprecation Notices
The legacy CloudNest Sync v1 desktop client was deprecated and no longer receives security updates.
Users on Sync v1 are prompted to upgrade to Sync v2, which supports selective sync and faster indexing.
The v1 public API (api.cloudnest.example/v1) will be sunset; new integrations should use v2 endpoints.
